# System Architecture & Design Document

## 📐 System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    AIRLINE MANAGEMENT SYSTEM                    │
└─────────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
                ▼             ▼             ▼
          ┌──────────┐  ┌──────────┐  ┌──────────┐
          │  ADMIN   │  │CUSTOMER  │  │  GUEST   │
          │INTERFACE │  │INTERFACE │  │  EXIT    │
          └──────┬───┘  └──────┬───┘  └──────────┘
                 │             │
        ┌────────┴────┐  ┌─────┴──────────┐
        │             │  │                │
        ▼             ▼  ▼                ▼
    ┌─────────┐  ┌──────────┐  ┌──────────────┐
    │ FLIGHT  │  │  USER    │  │  BOOKING     │
    │SERVICE  │  │SERVICE   │  │SERVICE       │
    └────┬────┘  └────┬─────┘  └──────┬───────┘
         │            │               │
    ┌────┴────┐   ┌───┴────┐   ┌─────┴─────┐
    │  Flight │   │  User  │   │  Booking  │
    │ Storage │   │Storage │   │ Storage   │
    │(HashMap)│   │HashMap │   │(HashMap)  │
    └─────────┘   └────────┘   └───────────┘
```

---

## 🏛️ Layered Architecture

```
┌────────────────────────────────────────────────────┐
│           PRESENTATION LAYER                       │
│  (Menu Interface - Scanner Input/System Output)    │
│  - Main.java                                        │
│  - AdminService (UI)                               │
│  - CustomerService (UI)                            │
└────────────────┬─────────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────────┐
│           BUSINESS LOGIC LAYER                    │
│  (Service Classes - Core Operations)              │
│  - AdminService (operations)                      │
│  - CustomerService (operations)                   │
│  - FlightService                                  │
│  - UserService                                    │
│  - BookingService                                 │
└────────────────┬─────────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────────┐
│           DATA ACCESS LAYER                       │
│  (Storage & Retrieval)                            │
│  - HashMap<String, Flight>                        │
│  - HashMap<String, User>                          │
│  - HashMap<String, Booking>                       │
└────────────────┬─────────────────────────────────┘
                 │
┌────────────────▼─────────────────────────────────┐
│           MODEL LAYER                             │
│  (Entity Classes)                                 │
│  - Flight.java                                    │
│  - User.java                                      │
│  - Booking.java                                   │
└────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Flight Booking Flow

```
START
  │
  ├─► User Login ──────────► Authentication
  │                              │
  │                              ▼
  ├─► Search Flights ─────────► FlightService
  │        │                        │
  │        └──────► Filter by  ────┴──► Return Matching Flights
  │               Source/Destination
  │
  ├─► Select Flight ──────────► Validate Flight Exists
  │        │
  │        ▼
  ├─► Check Availability ─────► FlightService.availableSeats
  │        │
  │        ├─ NO ──► Show Error ──────────────────┐
  │        │                                       │
  │        └─ YES                                  │
  │           │
  │           ▼
  ├─► Book Flight ──────────► BookingService
  │        │
  │        ├─► Reduce Available Seats (FlightService)
  │        │
  │        ├─► Create Booking Record
  │        │
  │        ├─► Assign Seat Number
  │        │
  │        ├─► Store in BookingService
  │        │
  │        ▼
  ├─► Display Confirmation ──► Show Booking ID & Details
  │        │
  │        ▼
  └─► DONE (Return to Menu)
```

---

## 🗂️ Class Diagram

```
┌──────────────────────┐
│      Flight          │
├──────────────────────┤
│ - flightId: String   │
│ - source: String     │
│ - destination: String│
│ - depTime: String    │
│ - arrTime: String    │
│ - totalSeats: int    │
│ - availableSeats: int│
│ - price: double      │
├──────────────────────┤
│ + getFlightId()      │
│ + bookSeat()         │
│ + cancelSeat()       │
│ + toString()         │
└──────────────────────┘

┌──────────────────────┐
│       User           │
├──────────────────────┤
│ - userId: String     │
│ - name: String       │
│ - email: String      │
│ - password: String   │
│ - phoneNumber: String│
├──────────────────────┤
│ + getUserId()        │
│ + getName()          │
│ + toString()         │
└──────────────────────┘

┌──────────────────────┐
│     Booking          │
├──────────────────────┤
│ - bookingId: String  │
│ - passengerName: Str │
│ - flightId: String   │
│ - seatNumber: String │
│ - bookingDate: Date  │
│ - status: String     │
├──────────────────────┤
│ + getBookingId()     │
│ + cancelBooking()    │
│ + toString()         │
└──────────────────────┘

┌─────────────────────────────┐
│   FlightService             │
├─────────────────────────────┤
│ - flights: HashMap          │
├─────────────────────────────┤
│ + addFlight()               │
│ + deleteFlight()            │
│ + updateFlight()            │
│ + searchFlights()           │
│ + getAllFlights()           │
│ + getFlightById()           │
└─────────────────────────────┘
           ▲
           │ uses
           │
        Flight

┌─────────────────────────────┐
│    UserService              │
├─────────────────────────────┤
│ - users: HashMap            │
├─────────────────────────────┤
│ + registerUser()            │
│ + loginUser()               │
│ + updateUser()              │
│ + getUserById()             │
└─────────────────────────────┘
           ▲
           │ uses
           │
          User

┌─────────────────────────────┐
│  BookingService             │
├─────────────────────────────┤
│ - bookings: HashMap         │
├─────────────────────────────┤
│ + bookFlight()              │
│ + cancelBooking()           │
│ + getBookingById()          │
│ + getPassengerBookings()    │
│ + getAllBookings()          │
└─────────────────────────────┘
      ▲         ▲
      │ uses    │ uses
      │         │
  Booking    Flight
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────┐
│            AUTHENTICATION MODULE                    │
└─────────────────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
   ADMIN AUTH              CUSTOMER AUTH
        │                         │
   Email + Password          Email + Password
        │                         │
        ▼                         ▼
   Hardcoded Check          HashMap Search
   (admin@airline.com)   (EmailExists + PasswordMatch)
        │                         │
   ┌────┴────┐              ┌─────┴─────┐
   │          │              │           │
 SUCCESS    FAIL          SUCCESS      FAIL
   │          │              │           │
   ▼          ▼              ▼           ▼
 Grant    Deny          Grant         Deny
 Access   Access         Access        Access
          Error          User          Error
                         Object
```

---

## 📊 State Diagram - Booking Lifecycle

```
┌─────────────────────────────────────────────┐
│           BOOKING STATES                    │
└─────────────────────────────────────────────┘

        [START]
          │
          ▼
    ┌───────────────┐
    │   NO BOOKING  │
    └───────┬───────┘
            │
            │ User selects flight & confirms
            │
            ▼
    ┌───────────────────┐
    │   BOOKING CREATED │ ◄─── Seat allocated
    └───────┬───────────┘      Booking ID generated
            │
            │
            ├─ User cancels       
            │         │
            │         ▼
            │    ┌──────────────┐
            │    │  CANCELLED   │ ◄─── Refund initiated
            │    │              │      Seat freed
            │    └──────────────┘
            │
            └─ User keeps booking
                      │
                      ▼
            ┌───────────────────┐
            │   CONFIRMED       │ ◄─── Payment confirmed
            │   (FINAL STATE)   │      Trip ready
            └───────────────────┘

        [POSSIBLE FINAL STATES]
     - CONFIRMED (booking complete)
     - CANCELLED (refunded)
```

---

## 🔀 Activity Diagram - Add Flight (Admin)

```
START (Admin logged in)
    │
    ▼
┌──────────────────────────┐
│ Show Add Flight Menu     │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Prompt Flight Details    │
│ - Flight ID              │
│ - Source                 │
│ - Destination            │
│ - Times                  │
│ - Seats                  │
│ - Price                  │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Validate Input           │
│ - Check empty values     │
│ - Check duplicates       │
│ - Check numeric values   │
└────────┬─────────────────┘
         │
    ┌────┴────┐
    │          │
 VALID     INVALID
    │          │
    ▼          ▼
  YES        ERROR
    │        MESSAGE
    │          │
    │          └─►(PROMPT AGAIN)
    │
    ▼
┌──────────────────────────┐
│ Create Flight Object     │
│ Add to FlightService     │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Show Success Message     │
│ Flight added!            │
└────────┬─────────────────┘
         │
         ▼
END (Return to Admin Menu)
```

---

## 🎯 Sequence Diagram - Customer Login & Booking

```
Customer    Main    CustomerService    UserService    FlightService    BookingService
    │       │              │               │                │                │
    │──────►│ Login Request │               │                │                │
    │       │              │               │                │                │
    │       │──────────────►│ loginUser()   │                │                │
    │       │              │               │                │                │
    │       │              │──────────────►│ Check Email    │                │
    │       │              │◄──────────────│ Return User    │                │
    │       │◄──────────────│ User Object   │                │                │
    │ Show Customer Menu    │               │                │                │
    │◄──────────────────────│               │                │                │
    │       │               │               │                │                │
    │──────►│ Search Flights│               │                │                │
    │       │              │───────────────────────────────►│ Search          │
    │       │              │◄───────────────────────────────│ Return Results  │
    │ Show Flight List      │               │                │                │
    │◄──────────────────────│               │                │                │
    │       │               │               │                │                │
    │──────►│ Book Ticket   │               │                │                │
    │       │              │───────────────────────────────────────────────►│
    │       │              │                │                │   Create Booking
    │       │              │                │                │   Update Seats
    │       │              │◄───────────────────────────────────────────────│
    │ Show Confirmation    │               │                │                │
    │◄──────────────────────│               │                │                │
    │       │               │               │                │                │
```

---

## 🗄️ Data Structure Diagram

```
┌─────────────────────────────────────────────┐
│      FlightService Data Store               │
├─────────────────────────────────────────────┤
│ HashMap<String, Flight>                     │
│                                             │
│  "FL001" ──► Flight {                      │
│              flightId: "FL001"              │
│              source: "Delhi"                │
│              destination: "Mumbai"          │
│              ...                            │
│          }                                  │
│                                             │
│  "FL002" ──► Flight {                      │
│              flightId: "FL002"              │
│              source: "Delhi"                │
│              destination: "Bangalore"       │
│              ...                            │
│          }                                  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│      UserService Data Store                 │
├─────────────────────────────────────────────┤
│ HashMap<String, User>                       │
│                                             │
│  "USER1001" ──► User {                     │
│                 userId: "USER1001"          │
│                 name: "Raj Kumar"           │
│                 email: "raj@email.com"      │
│                 ...                         │
│             }                               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│      BookingService Data Store              │
├─────────────────────────────────────────────┤
│ HashMap<String, Booking>                    │
│                                             │
│  "BK5001" ──► Booking {                    │
│               bookingId: "BK5001"           │
│               passengerName: "Raj Kumar"    │
│               flightId: "FL001"             │
│               status: "CONFIRMED"           │
│               ...                           │
│           }                                 │
└─────────────────────────────────────────────┘
```

---

## 🔌 Interface Design

```
┌──────────────────────────────────────────────────────┐
│              MAIN MENU INTERFACE                     │
├──────────────────────────────────────────────────────┤
│  1. Admin Login                                      │
│  2. Customer Portal                                  │
│  3. Exit                                             │
└──────────────────────────────────────────────────────┘
              │                    │
              ▼                    ▼
     ┌──────────────┐      ┌──────────────────┐
     │  ADMIN MENU  │      │ CUSTOMER MENU    │
     ├──────────────┤      ├──────────────────┤
     │1. Add Flight │      │1. Search Flights │
     │2. View All   │      │2. Book Ticket    │
     │3. Update     │      │3. View History   │
     │4. Delete     │      │4. Cancel Booking │
     │5. Details    │      │5. View Profile   │
     │6. Logout     │      │6. Logout         │
     └──────────────┘      └──────────────────┘
```

---

## 🔐 Security Architecture (Current)

```
AUTHENTICATION
     │
     ├─► ADMIN: Hardcoded credentials
     │         (admin@airline.com / admin123)
     │
     ├─► CUSTOMER: HashMap email/password lookup
     │            Password stored as plain text
     │            No hashing/encryption
     │
     └─► SESSION: In-memory User object
                  Lost on logout


AUTHORIZATION
     │
     ├─► ADMIN: Access to flight operations
     │
     └─► CUSTOMER: Access to booking operations


DATA STORAGE
     │
     └─► IN-MEMORY: HashMap (no encryption)
         Accessible in application runtime
         Lost on exit
```

---

## 📈 Scalability Analysis

### Current Limitations
```
Memory: O(n) where n = total entities
Search: O(n) for most operations
Concurrent Users: 1 (single-threaded)
Data Persistence: None (session-only)
```

### For Scalability
```
Memory: Use pagination
Search: Add database indexes
Concurrency: Thread pool with synchronization
Persistence: Database + caching layer
```

---

## 🚀 Deployment Architecture

```
Development Environment
    │
    ├─► Single JAR/Class files
    ├─► Local execution
    └─► In-memory storage

Production Environment
    │
    ├─► WAR file (web version)
    ├─► Docker container
    ├─► Database server
    ├─► Load balancer
    └─► Cloud hosting (AWS/Azure)
```

---

## 📝 Design Patterns Used

1. **Service Pattern**
   - Separation of business logic from UI

2. **DAO Pattern** (for future JDBC)
   - Abstraction of data access

3. **Factory Pattern** (implicit)
   - Service instantiation in Main

4. **Singleton Pattern** (implicit)
   - Single instance of services

5. **Model-View-Service Pattern**
   - Clear separation of concerns

---

## 🔄 Comparison: Current vs. Future Versions

```
FEATURE              CURRENT      FUTURE (v2.0)
────────────────────────────────────────────
Storage              HashMap      Database
Persistence          No           Yes
Concurrency          Single       Multi-threaded
UI                   Console      GUI/Web
Authentication       Basic        Advanced (JWT)
Deployment           Local        Cloud
Scalability          Limited      High
```

---

**This architecture provides a solid foundation for learning and can be extended into a production-grade system!**
